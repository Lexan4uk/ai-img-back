services:
  postgres:
    image: postgres:16
    container_name: ai-img-back
    environment:
      POSTGRES_DB: ai-img-back
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
