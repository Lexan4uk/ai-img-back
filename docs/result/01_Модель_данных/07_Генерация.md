# 07. Генерация (generation_batches + generation_requests)

## Назначение

Две таблицы фиксируют историю генерации изображений:

- **`generation_batches`** — один запуск генерации. Хранит промпт пользователя,
  параметры генерации, провайдер AI, режим дедупликации.
- **`generation_requests`** — одна пара (тип × стиль) внутри batch.
  Хранит хеш для дедупликации, статус выполнения и ссылку на созданный ассет.

---

## Структура таблиц

### generation_batches

```sql
CREATE TABLE generation_batches (
    id                BIGSERIAL PRIMARY KEY,
    owner_user_id     BIGINT    REFERENCES users(id) ON DELETE SET NULL,
    user_prompt       TEXT      NOT NULL,
    generation_params JSONB     NOT NULL DEFAULT '{}',
    dedupe_mode       TEXT      NOT NULL DEFAULT 'SKIP',
    provider          TEXT      NOT NULL,
    model             TEXT,
    routing_mode      TEXT      NOT NULL,
    CONSTRAINT ck_batches_dedupe  CHECK (dedupe_mode  IN ('SKIP', 'OVERWRITE')),
    CONSTRAINT ck_batches_routing CHECK (routing_mode IN ('DIRECT', 'PROXY'))
);
```

| Поле                | Тип       | Описание                                                         |
| ------------------- | --------- | ---------------------------------------------------------------- |
| `id`                | BIGSERIAL | PK                                                               |
| `owner_user_id`     | BIGINT    | Кто запустил (NULL если пользователь удалён)                     |
| `user_prompt`       | TEXT      | Текст промпта от пользователя                                    |
| `generation_params` | JSONB     | Параметры генерации: `{"aspectRatio": "1:1"}`                     |
| `dedupe_mode`       | TEXT      | `SKIP` — пропускать дубликаты, `OVERWRITE` — генерировать заново |
| `provider`          | TEXT      | Провайдер AI (по умолчанию `openai`)                             |
| `model`             | TEXT      | Модель AI (nullable, например `gpt-image-1.5`)                   |
| `routing_mode`      | TEXT      | `DIRECT` или `PROXY`                                             |

> **Статуса нет.** Вычисляется по COUNT реквестов:
> все DONE → batch DONE, есть FAILED → есть ошибки, и т.д.

### generation_requests

```sql
CREATE TABLE generation_requests (
    id                BIGSERIAL   PRIMARY KEY,
    batch_id          BIGINT      NOT NULL REFERENCES generation_batches(id) ON DELETE CASCADE,
    image_type_id     BIGINT      NOT NULL REFERENCES image_types(id),
    style_id          BIGINT      NOT NULL REFERENCES styles(id),
    final_prompt_hash TEXT        NOT NULL,
    status            TEXT        NOT NULL DEFAULT 'PENDING',
    created_asset_id  BIGINT      REFERENCES assets(id),
    error_message     TEXT,
    created_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT ck_requests_status CHECK (status IN ('PENDING','RUNNING','DONE','FAILED','SKIPPED'))
);
```

| Поле                | Тип         | Описание                               |
| ------------------- | ----------- | -------------------------------------- | --------------------- |
| `id`                | BIGSERIAL   | PK                                     |
| `batch_id`          | BIGINT      | Ссылка на batch (CASCADE при удалении) |
| `image_type_id`     | BIGINT      | Тип изображения для этой пары          |
| `style_id`          | BIGINT      | Стиль для этой пары                    |
| `final_prompt_hash` | TEXT        | SHA-256 от `finalPrompt + "            | " + generationParams` |
| `status`            | TEXT        | Текущий статус (см. ниже)              |
| `created_asset_id`  | BIGINT      | Ссылка на созданный ассет (если DONE)  |
| `error_message`     | TEXT        | Текст ошибки (если FAILED)             |
| `created_at`        | TIMESTAMPTZ | Время создания реквеста                |

---

## Жизненный цикл request

```
PENDING → RUNNING → DONE      (успех, создан asset)
                  → FAILED    (ошибка AI)
                  → SKIPPED   (дубликат + режим SKIP)
```

---

## API

```
POST /generations
UserId: <Long>
```

```json
{
	"userPrompt": "кузнец куёт меч",
	"generationParams": "{\"aspectRatio\": \"1:1\"}",
	"imageTypeIds": [2, 3],
	"styleIds": [5],
	"dedupeMode": "SKIP",
	"provider": "openai",
	"model": "gpt-image-1.5",
	"routingMode": "DIRECT"
}
```

| Поле               | Обязательно | По умолчанию |
| ------------------ | ----------- | ------------ |
| `userPrompt`       | ✅          | —            |
| `generationParams` | —           | `"{}"`       |
| `imageTypeIds`     | ✅          | —            |
| `styleIds`         | ✅          | —            |
| `dedupeMode`       | —           | `SKIP`       |
| `provider`         | —           | `openai`     |
| `model`            | —           | —            |
| `routingMode`      | —           | `DIRECT`     |

**Ответ** — массив по каждой паре (тип × стиль):

```json
[
	{
		"requestId": 42,
		"imageTypeId": 2,
		"styleId": 5,
		"status": "DONE",
		"createdAssetId": 123,
		"errorMessage": null
	}
]
```

---

## DTO

```java
class GenerateRequest {
    String userPrompt;          // @NotBlank
    String generationParams;    // nullable, JSON string
    List<Long> imageTypeIds;    // @NotEmpty
    List<Long> styleIds;        // @NotEmpty
    DedupeMode dedupeMode;      // nullable → default SKIP
    String provider;            // nullable → default "openai"
    String model;               // nullable
    RoutingMode routingMode;    // nullable → default DIRECT
}

class GenerationResultDTO {
    Long requestId;
    Long imageTypeId;
    Long styleId;
    RequestStatus status;       // DONE, SKIPPED, FAILED
    Long createdAssetId;        // nullable (null если SKIPPED/FAILED)
    String errorMessage;        // nullable (null если DONE/SKIPPED)
}
```

---

## Алгоритм генерации (v1 — последовательный)

1. **Валидация**: промпт не пустой, списки не пустые, нет `UNDEFINED` ID
2. **Загрузка справочников**: типы и стили по ID (404 если не найдены)
3. **Создание batch**: сохраняем промпт, params, provider, mode
4. **Формирование реквестов**: декартово произведение types × styles. Для каждой пары:
   - Собрать `finalPrompt` = `typePrompt + ". " + stylePrompt + ". " + userPrompt`
   - Посчитать `hash` = `SHA-256(finalPrompt + "|" + generationParams)`
   - `INSERT generation_request`
5. **Последовательное выполнение**: для каждого реквеста:
   - Проверить дубликат: `existsByHash(hash)` в generation_requests (глобально)
   - Если дубликат + SKIP → `status = SKIPPED`
   - Иначе: вызвать AI → сохранить asset → `status = DONE`
   - При ошибке: `status = FAILED`, `error_message`
6. **Ответ**: массив результатов

---

## Дедупликация

### Зачем нужна

Генерация изображения через AI стоит денег (~$0.02–0.19 за картинку). Если два пользователя (или один пользователь дважды) отправляют одинаковый запрос — нет смысла платить за генерацию повторно. Дедупликация экономит бюджет и ускоряет ответ.

### Как работает хеш

Каждый запрос на генерацию получает уникальный **отпечаток** — SHA-256 хеш. Хеш вычисляется из двух частей:

```
hash = SHA-256( finalPrompt + "|" + generationParams )
```

Где:
- `finalPrompt` — итоговый промпт, собранный из шаблона с подставленными типом, стилем и текстом пользователя
- `generationParams` — JSON с параметрами (aspect ratio и др.)

**Что это значит на практике:**

| Ситуация | Одинаковый хеш? | Результат (SKIP) |
| -------- | --------------- | ---------------- |
| Тот же промпт, тот же тип, тот же стиль, те же параметры | Да | Пропуск (бесплатно) |
| Тот же промпт, но **другой стиль** | Нет | Новая генерация |
| Тот же промпт, но **другой aspect ratio** | Нет | Новая генерация |
| Тот же промпт, **другой пользователь** | Да | Пропуск (бесплатно) |

Хеш хранится в поле `final_prompt_hash` таблицы `generation_requests`.

### Режимы дедупликации

Пользователь выбирает режим при отправке запроса:

**SKIP** (по умолчанию) — если в базе уже есть запрос с таким же хешем в статусе DONE, новый запрос получает статус SKIPPED. Генерация не происходит, деньги не тратятся. Ранее созданный asset уже существует и доступен.

**OVERWRITE** — генерация выполняется в любом случае, даже если дубликат уже есть. Создаётся новый asset. Полезно, когда пользователь хочет получить другой вариант картинки по тому же описанию.

### Область проверки

Дедупликация **глобальная** — проверяется по всем пользователям. Если пользователь A уже сгенерировал картинку с таким промптом, пользователь B получит SKIPPED (в режиме SKIP). Это максимально экономит бюджет.

### Пример

```
Пользователь отправляет: 3 типа × 2 стиля = 6 запросов.
Из них 2 уже были сгенерированы ранее.

Режим SKIP:  4 генерации (платно) + 2 SKIPPED (бесплатно)
Режим OVERWRITE: 6 генераций (все платно)
```

---

## Сборка промпта

Промпт формируется по настраиваемому шаблону (см. `ai.prompt-template` в application.properties):

```
Шаблон: "Create an image of type: {type}. Style: {style}. Description: {prompt}."

Подстановка:
  {type}   ← type_prompt из БД (или "любой" если пусто)
  {style}  ← style_prompt из БД (или "любой" если пусто)
  {prompt} ← текст пользователя

Результат:
  "Create an image of type: Photography. Style: Minimalism. Description: A blacksmith forging a sword."

Хеш:
  SHA-256("Create an image of type: Photography..." + "|" + '{"aspectRatio":"1:1"}')
  → "a3f8c1d2e5b7..."
```

---

## AI-провайдеры

Подробная документация по интеграции с AI-провайдерами (OpenAI, Stability AI),
настройке прокси, хранению файлов и обработке нештатных ситуаций — см.
[05_Интеграция_с_AI.md](../03_Архитектура/05_Интеграция_с_AI.md).
